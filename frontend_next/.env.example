# Default (recommended): same-origin through nginx reverse proxy
NEXT_PUBLIC_API_URL=/api/v1

# Optional: explicit external API URL (only if needed)
# NEXT_PUBLIC_API_URL=https://api.example.com/api/v1
